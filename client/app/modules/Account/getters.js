const Account = ['Account'];

const info = Account.concat(['info']);
const loggedin = info.concat(['email']);
const loginError = Account.concat(['loginError']);

export default {
  info,
	loggedin,
	loginError,
};
